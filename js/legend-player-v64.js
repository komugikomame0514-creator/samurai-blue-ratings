
(() => {
 const q=new URLSearchParams(location.search),era=window.YATAGARASU_ERAS.find(e=>e.year===Number(q.get("era"))),p=era?.players.find(x=>x.id===q.get("id"));if(!era||!p){location.href="legends.html";return}
 const $=id=>document.getElementById(id),labels=["スピード","シュート","パス","ドリブル","ディフェンス","フィジカル"];
 document.title=`${p.name} ${era.year} | PROJECT YATAGARASU`;$("legendPhoto").src=p.image;$("legendPhoto").alt=`${era.year} ${p.name}`;$("legendOvr").textContent=p.ovr;$("legendMeta").textContent=`${era.year} ${era.host} / ${p.pos} / ${p.legacy}`;$("legendName").textContent=p.name;$("legendEn").textContent=p.en;$("legendRole").textContent=p.role;$("legendNumber").textContent=`#${p.no}`;$("deepRole").textContent=p.role;$("deepReview").textContent=p.review;$("deepImpact").textContent=p.impact;$("deepLegacy").textContent=p.legacy;$("deepStrengths").innerHTML=p.strengths.map(x=>`<em>${x}</em>`).join("");$("deepIdentity").textContent=era.identity;
 $("legendStats").innerHTML=labels.map((n,i)=>`<div class="legend-stat-row"><span>${n}</span><div class="legend-stat-bar"><i style="width:${p.stats[i]}%"></i></div><b>${p.stats[i]}</b></div>`).join("");
 $("tournamentContext").innerHTML=`<div class="context-card"><span>TOURNAMENT STORY</span><b>${era.title}</b><p>${era.story}</p></div><div class="context-card"><span>TEAM RESULT</span><b>${era.stage} / ${era.record}</b></div><div class="context-card"><span>MATCHES</span><div class="match-list">${era.matches.map(m=>`<div class="match-row"><span>${m.date.slice(5)}</span><span>${m.stage} vs ${m.opponent}</span><b>${m.score}</b></div>`).join("")}</div><a class="official-link" href="${era.source}" target="_blank" rel="noopener">JFA公式ワールドカップヒストリーを開く</a><div class="photo-credit">写真はJFA公式ページに掲載された当時の大会画像を参照表示しています。</div></div>`;
 function draw(){const c=$("legendRadar"),r=c.getBoundingClientRect(),w=Math.max(420,Math.round(r.width||520)),h=Math.round(w*.78),d=Math.min(devicePixelRatio||1,2);c.width=w*d;c.height=h*d;c.style.width=`${w}px`;c.style.height="auto";const x=c.getContext("2d");x.setTransform(d,0,0,d,0,0);const cx=w/2,cy=h/2+4,R=Math.min(w,h)*.31;for(let l=1;l<=5;l++){const rr=R*l/5;x.beginPath();labels.forEach((_,i)=>{const a=-Math.PI/2+i*Math.PI*2/labels.length,px=cx+Math.cos(a)*rr,py=cy+Math.sin(a)*rr;i?x.lineTo(px,py):x.moveTo(px,py)});x.closePath();x.strokeStyle=l===5?"rgba(248,245,236,.78)":"rgba(164,187,224,.26)";x.lineWidth=l===5?2:1;x.stroke()}x.beginPath();p.stats.forEach((v,i)=>{const a=-Math.PI/2+i*Math.PI*2/p.stats.length,rr=R*v/100,px=cx+Math.cos(a)*rr,py=cy+Math.sin(a)*rr;i?x.lineTo(px,py):x.moveTo(px,py)});x.closePath();x.fillStyle="rgba(41,101,232,.42)";x.fill();x.strokeStyle="#8db8ff";x.lineWidth=3;x.stroke();labels.forEach((lab,i)=>{const a=-Math.PI/2+i*Math.PI*2/labels.length,rr=R+48,px=cx+Math.cos(a)*rr,py=cy+Math.sin(a)*rr;x.textAlign="center";x.fillStyle="#f8f5ec";x.font='700 13px "Yu Gothic UI"';x.fillText(lab,px,py-8);x.fillStyle="#d7b96b";x.fillText(p.stats[i],px,py+12)})}requestAnimationFrame(draw);addEventListener("resize",()=>{clearTimeout(window.__lr);window.__lr=setTimeout(draw,150)})
})();